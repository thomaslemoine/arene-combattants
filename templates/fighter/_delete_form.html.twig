<hr>
<form method="post" class="mb-4" action="{{ path('fighter_delete', {'slug': fighter.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fighter.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
