{% extends 'base.html.twig' %}

{% block title %}Battle{% endblock %}

{% block body %}

    <h1>Battle</h1>

    <table class="table">
        <thead>
        <tr>
            <th>ID</th>
            <th>Description</th>
            <th>Gagnant</th>
        </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ battle.id }}</td>
                <td>{{ battle.description|raw }}</td>
                <td>{{ battle.winner }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('battle_index') }}">back to list</a>

    <a href="{{ path('battle_edit', {'id': battle.id}) }}">edit</a>

    {{ include('battle/_delete_form.html.twig') }}
{% endblock %}
